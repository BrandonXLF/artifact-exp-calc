@tailwind base;

@layer base {
	html {
		line-height: 1;
	}

	body {
		@apply bg-main;
		@apply text-white;
		@apply m-4;
		@apply [color-scheme:dark];
		@apply [height:unset];
	}

	main {
		line-height: 1.5;
	}

	a {
		@apply text-blue-300;
	}

	a:focus,
	a:hover {
		@apply underline;
	}

	summary {
		@apply cursor-pointer;
		@apply select-none;
	}

	select,
	input[type='number'] {
		@apply px-1;
		@apply py-0.5;
		@apply border;
		@apply border-light;
		@apply rounded-md;
		@apply bg-input;
	}

	button {
		@apply border-none;
		@apply hover:bg-secondary;
		@apply hover:bg-secondary;
	}

	h3 {
		@apply mb-2;
	}
}

@tailwind components;
@tailwind utilities;
